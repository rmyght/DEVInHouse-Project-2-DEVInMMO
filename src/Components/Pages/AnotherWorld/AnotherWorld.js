import { HeaderAnother } from "./AnotherWorld.styles";

// Componente da página que não está "mapeada"

export const AnotherWorld = () => {
    return (
        <HeaderAnother>There is literally nothing to see here (Maybe this text?)</HeaderAnother>
    );
};