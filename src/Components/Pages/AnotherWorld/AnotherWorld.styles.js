import styled from "styled-components";

// Styled Components do AnotherWorld

export const HeaderAnother = styled.h1`
  color: ${({ theme }) => theme.colors.main};
  display: flex;
  align-items: center;
  justify-content: center;
`;